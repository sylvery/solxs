{% extends 'base.html.twig' %}

{% block title %}Design index{% endblock %}

{% block body %}
    <h1>Design index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Description</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for design in designs %}
            <tr>
                <td>{{ design.id }}</td>
                <td>{{ design.name }}</td>
                <td>{{ design.description }}</td>
                <td>
                    <a href="{{ path('design_show', {'id': design.id}) }}">show</a>
                    <a href="{{ path('design_edit', {'id': design.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('design_new') }}">Create new</a>
{% endblock %}
