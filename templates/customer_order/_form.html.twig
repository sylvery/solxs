{{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="btn-group">
        <button class="btn btn-sm btn-success small">{{ button_label|default('Save') }}</button>
        <a class="btn btn-sm btn-secondary small" href="{{ path('customer_order_index') }}">back to list</a>
    </div>
{{ form_end(form) }}
